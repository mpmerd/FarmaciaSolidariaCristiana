@{
    ViewData["Title"] = "Control de Mantenimiento";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="card shadow">
                <div class="card-header bg-primary text-white">
                    <h3 class="mb-0">
                        <i class="bi bi-wrench"></i> Control de Modo Mantenimiento
                    </h3>
                </div>
                <div class="card-body">
                    @if (TempData["SuccessMessage"] != null)
                    {
                        <div class="alert alert-success alert-dismissible fade show" role="alert">
                            <i class="bi bi-check-circle-fill"></i> @TempData["SuccessMessage"]
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                        </div>
                    }
                    
                    @if (TempData["ErrorMessage"] != null)
                    {
                        <div class="alert alert-danger alert-dismissible fade show" role="alert">
                            <i class="bi bi-exclamation-triangle-fill"></i> @TempData["ErrorMessage"]
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                        </div>
                    }

                    @if (ViewBag.IsMaintenanceMode)
                    {
                        <!-- Modo Mantenimiento ACTIVO -->
                        <div class="alert alert-warning border-warning mb-4">
                            <h4 class="alert-heading">
                                <i class="bi bi-exclamation-triangle-fill"></i> Modo Mantenimiento Activo
                            </h4>
                            <p class="mb-3">
                                El sitio está actualmente en modo mantenimiento. Los usuarios regulares verán una página de mantenimiento.
                                Los administradores pueden seguir accediendo normalmente.
                            </p>
                            <hr>
                            <p class="mb-0">
                                <strong>Motivo:</strong> @ViewBag.Reason
                            </p>
                        </div>

                        <form method="post" asp-action="Disable" class="text-center">
                            @Html.AntiForgeryToken()
                            <button type="submit" class="btn btn-success btn-lg" onclick="return confirm('¿Está seguro de desactivar el modo mantenimiento? Los usuarios podrán acceder nuevamente al sitio.');">
                                <i class="bi bi-check-circle"></i> Desactivar Modo Mantenimiento
                            </button>
                        </form>
                    }
                    else
                    {
                        <!-- Modo Mantenimiento INACTIVO -->
                        <div class="alert alert-success border-success mb-4">
                            <h4 class="alert-heading">
                                <i class="bi bi-check-circle-fill"></i> Sitio Funcionando Normalmente
                            </h4>
                            <p class="mb-0">
                                El sitio está operando normalmente. Todos los usuarios pueden acceder sin restricciones.
                            </p>
                        </div>

                        <div class="card bg-light">
                            <div class="card-body">
                                <h5 class="card-title">
                                    <i class="bi bi-exclamation-triangle"></i> Activar Modo Mantenimiento
                                </h5>
                                <p class="card-text text-muted">
                                    Active el modo mantenimiento cuando necesite realizar trabajos en la farmacia o mantenimiento del sistema.
                                    Los usuarios regulares verán una página informativa, pero los administradores seguirán teniendo acceso completo.
                                </p>
                                
                                <form method="post" asp-action="Enable">
                                    @Html.AntiForgeryToken()
                                    <div class="mb-3">
                                        <label for="reason" class="form-label">Motivo del Mantenimiento <span class="text-danger">*</span></label>
                                        <textarea class="form-control" id="reason" name="reason" rows="4" 
                                                  placeholder="Ej: Trabajos en la farmacia, actualización del sistema, inventario, etc." 
                                                  required></textarea>
                                        <div class="form-text">
                                            Este mensaje será visible para los usuarios en la página de mantenimiento.
                                        </div>
                                    </div>
                                    
                                    <div class="d-grid gap-2">
                                        <button type="submit" class="btn btn-warning btn-lg" 
                                                onclick="return confirm('¿Está seguro de activar el modo mantenimiento? Los usuarios regulares no podrán acceder al sitio.');">
                                            <i class="bi bi-wrench"></i> Activar Modo Mantenimiento
                                        </button>
                                    </div>
                                </form>
                            </div>
                        </div>
                    }

                    <div class="mt-4 text-center">
                        <a href="@Url.Action("Index", "Home")" class="btn btn-secondary">
                            <i class="bi bi-arrow-left"></i> Volver al Inicio
                        </a>
                    </div>
                </div>
            </div>

            <!-- Información adicional -->
            <div class="card mt-3 shadow-sm">
                <div class="card-body">
                    <h6 class="card-subtitle mb-2 text-muted">
                        <i class="bi bi-info-circle"></i> Información Importante
                    </h6>
                    <ul class="mb-0">
                        <li>Los usuarios con rol <strong>Admin</strong> siempre tendrán acceso completo al sitio.</li>
                        <li>El estado del mantenimiento se guarda en un archivo y persiste entre reinicios del servidor.</li>
                        <li>Use este modo cuando necesite realizar trabajos que no requieren detener el servicio web completamente.</li>
                        <li>Proporcione un motivo claro para que los usuarios entiendan la situación.</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        // Auto-dismiss alerts after 5 seconds
        setTimeout(function() {
            $('.alert').fadeOut('slow');
        }, 5000);
    </script>
}
