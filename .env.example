# ============================================
# Farmacia Solidaria Cristiana
# Ejemplo de Variables de Entorno
# ============================================
#
# INSTRUCCIONES:
# 1. Copia este archivo como .env
# 2. Rellena con tus datos reales
# 3. NUNCA compartas el archivo .env
#
# ============================================

# Base de Datos
DB_SERVER=localhost
DB_PORT=1433
DB_NAME=FarmaciaDb
DB_USER=tu_usuario
DB_PASSWORD=tu_password_seguro

# Cadena de Conexión Completa (alternativa)
CONNECTION_STRING=Server=${DB_SERVER},${DB_PORT};Database=${DB_NAME};User Id=${DB_USER};Password=${DB_PASSWORD};TrustServerCertificate=True;

# Credenciales Admin (Primera Ejecución)
ADMIN_EMAIL=admin@farmacia.local
ADMIN_USERNAME=admin
ADMIN_PASSWORD=cambia_esto_por_contraseña_segura

# Configuración de Aplicación
ASPNETCORE_ENVIRONMENT=Production
ASPNETCORE_URLS=http://0.0.0.0:5000

# API Externa
CIMA_API_BASE_URL=https://cima.aemps.es/cima/rest
CIMA_API_TIMEOUT=30

# Logging
LOG_LEVEL=Information
LOG_PATH=/var/log/farmacia/

# ============================================
# NOTAS DE SEGURIDAD:
# ============================================
# - Usa contraseñas fuertes (mínimo 12 caracteres)
# - No uses contraseñas por defecto
# - Cambia todas las credenciales después del primer despliegue
# - Mantén este archivo fuera del control de versiones
# - Usa permisos restrictivos: chmod 600 .env
# ============================================
